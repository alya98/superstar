pipeline:
  run-tests:
    image: node:latest
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    comands:
      - npm install --silent
      - npm run lint
    when:
      event: [pull_request]

branches: [ master, "*:master" ]